# FORGE VST3

Windows x64 VST3 build for FORGE, a metalcore/post-hardcore vocal producer.

CI builds the real VST3, standalone app, packaged offline engine, validates with pluginval, and creates a Windows installer.
